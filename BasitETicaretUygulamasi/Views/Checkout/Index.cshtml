@{
    ViewBag.Title = "Siparişi Tamamla";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="alert alert-success text-center p-5 shadow-sm rounded-4">
                <div class="mb-4">
                    <i class="fas fa-check-circle fa-4x text-success"></i>
                </div>
                <h2 class="fw-bold mb-3">Siparişiniz Alındı!</h2>
                <p class="lead mb-2">Ödeme sayfasına yönlendiriliyorsunuz...</p>
                <p class="text-muted small">(Bu bir simülasyondur, gerçek ödeme işlemi yapılmamaktadır.)</p>

                <a href="@Url.Action("Index", "Category")" class="btn btn-primary mt-4 px-4 py-2">
                    <i class="fas fa-home me-2"></i> Anasayfaya Dön
                </a>
            </div>
        </div>
    </div>
</div>
